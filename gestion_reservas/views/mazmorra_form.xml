<odoo>
    <data>

        <!-- Vista de formulario para el modelo 'res.mazmorra' -->
        <record id="view_mazmorra_form" model="ir.ui.view">
            <field name="name">mazmorra.form</field>
            <field name="model">res.mazmorra</field>
            <field name="arch" type="xml">
                <form string="Mazmorra">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="descripcion"/>
                            <field name="reserva_id" widget="one2many_list"/>
                        </group>
                    </sheet>
                    <footer>
                        <button string="Guardar" type="object" name="create"/>
                        <button string="Cancelar" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</odoo>
