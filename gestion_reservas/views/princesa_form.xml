<odoo>
    <data>

        <!-- Vista de formulario para el modelo 'res.princesa' -->
        <record id="view_princesa_form" model="ir.ui.view">
            <field name="name">princesa.form</field>
            <field name="model">res.princesa</field>
            <field name="arch" type="xml">
                <form string="Princesa">
                    <sheet>
                        <group>
                            <field name="name" string="Nombre de la Princesa" required="True"/>
                            <field name="descripcion" string="Descripción"/>
                        </group>
                        <group>
                            <field name="reserva_id" string="Reserva Asociada" widget="many2one"/>
                        </group>
                    </sheet>
                    <footer>
                        <button string="Guardar" type="object" name="create" class="btn-primary"/>
                        <button string="Cancelar" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</odoo>
