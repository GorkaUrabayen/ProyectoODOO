<odoo>
    <data noupdate="1">
        <!-- Ejecutar la función cada hora -->
        <record id="cron_cancelar_reservas" model="ir.cron">
            <field name="name">Cancelar reservas no confirmadas</field>
            <field name="model_id" ref="model_res_booking"/>
            <field name="state">code</field>
            <field name="code">model.cancelar_reserva_automatica()</field>
            <field name="interval_type">hours</field>
            <field name="interval_number">1</field>
            <field name="priority">5</field>
        </record>
    </data>
</odoo>
